{
  "name": "@org/catalog-service",
  "projectType": "application",
  "sourceRoot": "apps/catalog-service/src",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "dependsOn": ["^build"],
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/catalog-service",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/catalog-service/src/main.ts",
        "tsConfig": "apps/catalog-service/tsconfig.app.json",
        "assets": ["apps/catalog-service/src/assets"],
        "generatePackageJson": false,
        "esbuildOptions": {
          "sourcemap": true
        }
      },
      "configurations": {
        "development": {
          "sourcemap": true
        },
        "production": {
          "sourcemap": false,
          "minify": true
        }
      }
    },

    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "@org/catalog-service:build",
        "watch": true
      },
      "configurations": {
        "development": {
          "buildTarget": "@org/catalog-service:build:development"
        },
        "production": {
          "buildTarget": "@org/catalog-service:build:production"
        }
      }
    },

    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },

"test": {
  "executor": "@nx/jest:jest",
  "outputs": [
    "{workspaceRoot}/coverage/{projectRoot}"
  ],
  "options": {
    "jestConfig": "apps/catalog-service/jest.config.cts",
    "passWithNoTests": true
  }
}
  },
   "dependencies": {
    "express": "^4.21.2"
  }
}