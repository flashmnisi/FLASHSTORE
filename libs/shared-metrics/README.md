# shared-metrics

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-metrics` to build the library.

## Running unit tests

Run `nx test shared-metrics` to execute the unit tests via [Jest](https://jestjs.io).
