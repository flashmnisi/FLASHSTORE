# shared-telemetry

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-telemetry` to build the library.

## Running unit tests

Run `nx test shared-telemetry` to execute the unit tests via [Jest](https://jestjs.io).
